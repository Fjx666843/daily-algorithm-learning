​
题目链接：E-小月的相交弦_牛客周赛 Round 160

题目大意：

你有一根从1到 2n 的坐标轴，上面有 n 条“弦”（可以理解为连接两个端点的线段或弧）。每条弦有三个属性：

左端点 l：弦的开始位置

右端点 r：弦的结束位置

权值 w：选择这条弦能得到的分数


关键规则：

所有弦的端点（左右端点加起来共 2n 个）都互不相同，恰好占满1到 2n 的每个整数。

两条弦 (l_a, r_a) 和 (l_b, r_b) 如果满足 l_a < l_b < r_a < r_b，就称它们相交（可以想象成两条弦在圆圈内交叉了）。

你需要选出若干条弦，使得任意两条选中的弦都不相交（即不能有交叉），并且选出的弦的权值之和最大。

你可以一条都不选（此时权值和为0）。



题目思路：

由于每条弦的端点都在 [1, 2n] 上且互不重复，整个区间可以看作一个线性的坐标轴。两条弦 (l_a, r_a) 和 (l_b, r_b)（假设 l_a < l_b）相交，当且仅当它们交叉嵌套：l_a < l_b < r_a < r_b。这其实是括号匹配中“交叉”的定义。

因此，我们可以使用区间动态规划来解决。

状态定义
定义 dp[l][r] 表示：只考虑完全位于区间 [l, r] 内部的弦（即左端点和右端点都在 [l, r] 内），能够获得的最大权值和。

状态转移
对于区间 [l, r]，我们考虑左端点 l 这个位置，它有两种情况：

不选左端点为 l 的弦：那么答案直接继承自子区间，即 dp[l][r] = dp[l+1][r]。

选择左端点为 l 的弦：假设这条弦的右端点为 k（即弦为 (l, k)）。因为选择了它，根据不相交的定义，任何与它相交的弦都不能选。

与它不相交的弦，只能完全位于它的内部 (l, k)，或者完全位于它的外部 (k, r)。

由于弦的两个端点互不相同，区间 [l, k] 和 [k, r] 是独立的，不会互相影响。

所以，选择弦 (l, k) 时的最大权值和为：w(l, k) + dp[l+1][k-1] + dp[k+1][r]




因此，状态转移方程为：

dp[l][r] = max(
    dp[l+1][r],  // 不选 l 点
    w(l, k) + dp[l+1][k-1] + dp[k+1][r]  // 选择以 l 为左端点的弦 (l, k)，k 是其右端点
)

最终答案
我们要求解整个区间 [1, 2n] 的最优值，即 dp[1][2n]。

代码如下：

​#include <bits/stdc++.h>
using namespace std;
using in128 = __int128_t;
//#define int long long
using int64 = long long;
void solve()
{
    int n;
    cin >> n;
    int m = 2 * n;
    vector<int> rightOf(m + 1, 0);//以l为左端点的弦的右端点是什么
    vector<int64> val(m + 1, 0);//以l为左端点的弦的权值
    for (int i = 0; i < n;i++){
        int l, r;
        int64 w;
        cin >> l >> r >> w;
        rightOf[l] = r;
        val[l] = w;
    }
    vector<vector<int64>> dp(m + 2, vector<int64>(m + 2, 0));
    //按区间长度从小到大枚举
    //len表示区间长度（包含端点的个数），最短为2
    for (int len = 2; len <= m; len++)
    {
        // 枚举所有长度为 len 的区间 [l, r]
        for (int l = 1; l + len - 1 <= m; l++)
        {
            int r = l + len - 1; // 计算右端点

            // ===== 情况1：不选择以 l 为左端点的弦 =====
            // 如果 l 点没有被任何选中的弦作为左端点，那么它相当于被"跳过"
            // 最优值就直接继承自子区间 [l+1, r]
            dp[l][r] = dp[l + 1][r];

            // ===== 情况2：选择以 l 为左端点的弦 =====
            int rp = rightOf[l]; // 获取以 l 为左端点的弦的右端点
            // 如果存在这样的弦 (l, rp)，并且 rp 不超过当前区间的右端点 r
            // （如果 rp > r，说明这条弦超出了当前区间，不能在本区间内考虑）
            if (rp != 0 && rp <= r)
            {
                // 如果选择了这条弦 (l, rp)，那么：
                // 1. 获得它的权值 val[l]
                // 2. 在它内部，区间 [l+1, rp-1] 的弦可以自由选择（不相交）
                // 3. 在它外部，区间 [rp+1, r] 的弦也可以自由选择（不相交）
                // 内部和外部是独立的，所以累加最优值
                int64 cur = val[l] + dp[l + 1][rp - 1] + dp[rp + 1][r];

                // 取"不选"和"选"两种情况的最大值
                dp[l][r] = max(dp[l][r], cur);
            }
        }
    }
    // 最终答案就是整个区间 [1, m] 的最优值
    cout << dp[1][m] << endl;
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int T=1;
    //cin >> T;
    while (T--)
    {
        solve();
    }

    return 0;
}
